package com.techiefinder;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class TechieFinderApplicationTests {

    @Test
    void contextLoads() {
        // Fails if the Spring context (JPA mappings, Security config, bean wiring) doesn't start.
    }
}
